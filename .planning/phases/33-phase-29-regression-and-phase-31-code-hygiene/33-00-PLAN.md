---
phase: 33-phase-29-regression-and-phase-31-code-hygiene
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/src/screens/PlannerScreen.tsx
  - app/src/screens/SettingsScreen.tsx
  - app/src/screens/settings/SettingsDataScreen.tsx
  - app/src/screens/settings/SettingsFeaturesScreen.tsx
  - app/src/services/daily-read.service.ts
  - app/src/services/post-history.service.ts
  - app/tests/concept-quota.test.mjs
  - app/tests/services/concept-batch-filter.test.mjs
  - app/tests/services/daily-generation-cap.test.mjs
  - app/tests/services/starter-posts.test.mjs
autonomous: true
requirements: [WIP-FLUSH]
must_haves:
  truths:
    - "Working tree is clean — no uncommitted WIP when downstream waves begin"
    - "3 previously-untracked Phase-31 test suites run under node --test and each pass"
    - "WIP commit message reflects Phase 31 UAT-feedback polish, not a mixed-intent commit"
  artifacts:
    - path: "app/tests/services/concept-batch-filter.test.mjs"
      provides: "Phase 31 D-13 concept-filter test coverage (was untracked)"
      contains: "describe"
    - path: "app/tests/services/daily-generation-cap.test.mjs"
      provides: "Phase 31 D-38 generation-cap test coverage (was untracked)"
      contains: "describe"
    - path: "app/tests/services/starter-posts.test.mjs"
      provides: "Phase 31 D-43 starter-post test coverage (was untracked)"
      contains: "describe"
  key_links:
    - from: "git status --porcelain"
      to: "empty string"
      via: "single chore commit"
      pattern: "^$"
---

<objective>
Flush all uncommitted WIP from the working tree before Wave 1 starts, so downstream rename/delete tasks don't collide with experimental edits. Per researcher (33-RESEARCH.md Finding #7): all 12 files are safe to commit — 9 modified files carry Phase 31 UAT-feedback polish (i18n fixes, screen refinements, a quota refactor), 3 untracked files are Phase 31 bug-fix test suites that each pass locally.

Purpose: D-19/D-20/D-21 — a clean working tree is the prerequisite for TD-06's atomic rename commit per D-15.
Output: One `chore(v1.4)` commit; `git status --porcelain` returns empty; 28 newly-covered tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Review each WIP diff, run the 3 untracked test suites, and confirm clean disposition</name>
  <read_first>
    - app/src/services/daily-read.service.ts (behavioral change — review carefully)
    - app/tests/concept-quota.test.mjs (paired with daily-read change — must match)
    - app/src/screens/settings/SettingsDataScreen.tsx (i18n conversion)
    - app/src/locales/en.json (paired i18n additions — scan home.feed.scrollToTop, common.buttons.resetToday, common.toast.todayReset)
    - app/tests/services/concept-batch-filter.test.mjs (untracked — will be committed)
    - app/tests/services/daily-generation-cap.test.mjs (untracked — will be committed)
    - app/tests/services/starter-posts.test.mjs (untracked — will be committed)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #7 (expected disposition per file)
  </read_first>
  <action>
    Step 1 — Run `cd /Users/Code/EchoLearn/app && git status --porcelain` and confirm the output matches the expected 12-file set (9 modified + 3 untracked) plus `.planning/STATE.md`. If any file is unexpected, HALT and escalate.

    Step 2 — For each of the 9 modified files in the researcher's table (Finding #7), run `git diff -- <path>` and confirm the diff matches the described shape:
    - 4 locale bundles: 3-key addition only (resetToday button + todayReset toast + home.feed.scrollToTop)
    - `PlannerScreen.tsx`: 1-line removal of unused `useEffect` import
    - `SettingsScreen.tsx`: 1-line removal of unused `Palette` import from `lucide-react`
    - `SettingsDataScreen.tsx`: hardcoded-string to t() conversion for "Reset Today" button + toast
    - `SettingsFeaturesScreen.tsx`: `const [reviewLimit, setReviewLimit] = ...` → `const [reviewLimit] = ...`
    - `daily-read.service.ts`: getConceptQuota refactor (derive from questionsById anchors; rename EXCLUDED_SOURCE_TYPES → NON_CONCEPT_SOURCE_TYPES as `['starter', 'connection', 'suggestion']`)
    - `post-history.service.ts`: 1-line removal of dead `const DEFAULT_RETENTION_DAYS = 7;`
    - `concept-quota.test.mjs`: rewrite matching the daily-read refactor
    If any diff contains unexpected content (console.log, debug prints, secrets like `sk-` / `api_key`, TODO comments, half-commented code), HALT and escalate.

    Step 3 — Run the 3 untracked test files to confirm they each pass:
    ```
    cd /Users/Code/EchoLearn/app && node --test tests/services/concept-batch-filter.test.mjs tests/services/daily-generation-cap.test.mjs tests/services/starter-posts.test.mjs
    ```
    Expected: 28 tests pass total (8 + 11 + 9 per Finding #7). If any fails, HALT and escalate — do not commit failing tests.

    Step 4 — Run the paired `concept-quota.test.mjs`:
    ```
    cd /Users/Code/EchoLearn/app && node --test tests/concept-quota.test.mjs
    ```
    Expected: 8 tests pass (matches the refactored daily-read.service.ts shape).
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/services/concept-batch-filter.test.mjs tests/services/daily-generation-cap.test.mjs tests/services/starter-posts.test.mjs tests/concept-quota.test.mjs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` lists exactly the 12 files from 33-RESEARCH.md Finding #7 (plus `.planning/STATE.md`).
    - Each of the 9 modified diffs matches the researcher-described shape; no surprise content.
    - Targeted run of the 3 untracked test files + `concept-quota.test.mjs` reports "tests 36 / pass 36 / fail 0" (or equivalent — at minimum `fail 0`).
    - No HALT condition triggered (no secrets, no debug logs, no half-commented code).
  </acceptance_criteria>
  <done>All 12 files reviewed and categorized safe-to-commit; 36 targeted tests green.</done>
</task>

<task type="auto">
  <name>Task 0.2: Stage all 12 files plus STATE.md as one atomic chore commit</name>
  <read_first>
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md (D-19 commit message + D-20 review policy)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #7 Open Question 3 (STATE.md disposition)
  </read_first>
  <action>
    Step 1 — Stage only the 12 WIP files + STATE.md (do NOT use `git add -A` to avoid accidentally including phase-33 planning artifacts that will be committed later):

    ```
    cd /Users/Code/EchoLearn && git add \
      app/src/locales/en.json \
      app/src/locales/zh.json \
      app/src/locales/es.json \
      app/src/locales/ja.json \
      app/src/screens/PlannerScreen.tsx \
      app/src/screens/SettingsScreen.tsx \
      app/src/screens/settings/SettingsDataScreen.tsx \
      app/src/screens/settings/SettingsFeaturesScreen.tsx \
      app/src/services/daily-read.service.ts \
      app/src/services/post-history.service.ts \
      app/tests/concept-quota.test.mjs \
      app/tests/services/concept-batch-filter.test.mjs \
      app/tests/services/daily-generation-cap.test.mjs \
      app/tests/services/starter-posts.test.mjs \
      .planning/STATE.md
    ```

    Step 2 — Verify `git status --porcelain` now shows only these 15 lines staged as `M ` / `A ` (and no unexpected adds). Run `git diff --cached --stat` and visually confirm line counts are modest (no blob additions > 500 LOC per file except locale bundles, which are fine).

    Step 3 — Create the commit via HEREDOC (per D-19 exact message, extended per researcher Open Question 3 recommendation):

    ```
    git commit -m "$(cat <<'EOF'
    chore(v1.4): flush WIP — quota refactor, i18n polish, 3 Phase 31 test suites

    Flushes uncommitted working-tree state from Phase 31 UAT-feedback polish:
    - daily-read.service.ts: getConceptQuota derives quota from questionsById anchors
      (Phase 31 D-12 alignment); rename EXCLUDED_SOURCE_TYPES -> NON_CONCEPT_SOURCE_TYPES
    - concept-quota.test.mjs: rewrite to match new shape (8/8 pass)
    - i18n polish: resetToday button + todayReset toast + home.feed.scrollToTop keys
      added to en/zh/es/ja (bundle parity preserved)
    - SettingsDataScreen: hardcoded Reset Today strings -> t() calls
    - PlannerScreen / SettingsScreen: drop unused imports
    - SettingsFeaturesScreen: drop unused setReviewLimit setter
    - post-history.service.ts: drop dead DEFAULT_RETENTION_DAYS const
    - 3 new Phase 31 test suites (previously untracked, all pass locally):
        concept-batch-filter.test.mjs (8/8, Phase 31 D-13)
        daily-generation-cap.test.mjs (11/11, Phase 31 D-38)
        starter-posts.test.mjs (9/9, Phase 31 D-43)
    - STATE.md: Phase 33 context capture

    Prerequisite flush for Phase 33 TD-06 rename (D-19/D-20/D-21).
    EOF
    )"
    ```

    Step 4 — Run `git status --porcelain` and assert the output is EMPTY.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && git status --porcelain | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - Commit created with subject line exactly `chore(v1.4): flush WIP — quota refactor, i18n polish, 3 Phase 31 test suites`.
    - `git status --porcelain | wc -l` returns `0` (working tree clean).
    - `git log -1 --format=%s` returns the subject line above.
    - `git log -1 --stat | grep -c "app/"` returns at least 10 (matching the 14 app/ files staged).
  </acceptance_criteria>
  <done>Working tree clean; single commit recorded; downstream waves unblocked.</done>
</task>

</tasks>

<verification>
Combined phase 33-00 checks:
- `cd /Users/Code/EchoLearn && git status --porcelain` returns empty string.
- `cd /Users/Code/EchoLearn/app && node --test tests/services/concept-batch-filter.test.mjs tests/services/daily-generation-cap.test.mjs tests/services/starter-posts.test.mjs` — 28/28 pass.
- `cd /Users/Code/EchoLearn/app && node --test tests/concept-quota.test.mjs` — 8/8 pass.
- `git log -1 --format=%s` returns the Phase 33 WIP flush subject line.
</verification>

<success_criteria>
- `git status --porcelain` is empty.
- Exactly one new commit landed on the branch with subject matching D-19's specified message.
- 36 targeted tests green; no surprise regressions in the paired services.
</success_criteria>

<output>
After completion, create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-00-SUMMARY.md` recording the commit SHA, file list, and test counts.
</output>
