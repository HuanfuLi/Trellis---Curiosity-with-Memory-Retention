---
phase: 33-phase-29-regression-and-phase-31-code-hygiene
plan: 03
type: execute
wave: 2
depends_on: [33-01, 33-02]
files_modified:
  - app/src/services/trellis-state.service.ts
  - app/src/services/concept-feed.service.ts
  - app/src/screens/PlannerScreen.tsx
  - app/src/components/trellis/TrellisStatusPanel.tsx
  - app/src/components/trellis/TrellisLeaf.tsx
  - app/src/components/trellis/TrellisTooltip.tsx
  - app/tests/services/trellis-state.test.mjs
  - app/tests/components/trellis-tooltip-copy.test.mjs
  - app/tests/services/concept-batch-filter.test.mjs
autonomous: true
requirements: [TD-06]
must_haves:
  truths:
    - "LeafState type union reads 'bud | green | dying | falling | dead | blossom | fruit' — 'yellow' and 'fallen' no longer members"
    - "'falling' literal retained in the type union (per D-12 — internal-only gradation, UI never exposes it)"
    - "computeLeafState returns 'dying' for 1d overdue input and 'dead' for 14d overdue / ease<1.5 input"
    - "concept-feed.service.ts weak-concept predicate reads 'leaf === dying || leaf === falling || leaf === dead' — previously dead 'dying' literal (pre-rename: 'yellow') is now valid under the new union"
    - "TrellisLeaf.tsx STATE_COLOR and STATE_VEIN Records have renamed keys: dying and dead replace yellow and fallen (4 keys total)"
    - "TrellisTooltip.tsx resolveHealthCopy switch branches accept 'dying' / 'dead' arguments (matches pinned tests)"
    - "tsc -b --noEmit exit 0 after the rename"
    - "Badge.tsx remains untouched — 'yellow' there is a CSS color name, not LeafState"
    - "PodcastScreen.tsx:220 remains untouched — 'yellow' there is a UI status color, not LeafState"
    - "Single atomic commit contains the rename — git log --follow traces cleanly"
  artifacts:
    - path: "app/src/services/trellis-state.service.ts"
      provides: "LeafState type union with renamed literals; computeLeafState returning renamed literals; dev-mode seeds updated"
      must_contain: "'bud' | 'green' | 'dying' | 'falling' | 'dead' | 'blossom' | 'fruit'"
      must_not_contain: "'yellow'"
    - path: "app/src/components/trellis/TrellisLeaf.tsx"
      provides: "STATE_COLOR and STATE_VEIN Records keyed by new LeafState names"
      must_contain: "dying:"
      must_contain_second: "dead:"
  key_links:
    - from: "app/src/services/concept-feed.service.ts leaf === 'dying' predicate"
      to: "trellis-state.service.ts LeafState type"
      via: "TypeScript type-checking"
      pattern: "leaf === 'dying' \\|\\| leaf === 'falling' \\|\\| leaf === 'dead'"
    - from: "app/tests/components/trellis-tooltip-copy.test.mjs"
      to: "TrellisTooltip.tsx resolveHealthCopy"
      via: "test arg values 'dying' and 'dead'"
      pattern: "resolveHealthCopy\\('dying'"
---

<objective>
Unify the internal LeafState vocabulary with the user-facing design vocabulary. Per D-10/D-11/D-12/D-13/D-15:

- Rename `'yellow'` → `'dying'` across the entire codebase's LeafState contexts.
- Rename `'fallen'` → `'dead'` across the entire codebase's LeafState contexts.
- KEEP `'falling'` as-is (internal-only 7-13d-overdue gradation; UI never exposes it).
- Use ONE atomic commit per D-15 so `git log --follow` traces the rename cleanly.

**Scope boundaries:**
- DO NOT touch `app/src/components/ui/Badge.tsx` — `'yellow'` there is a CSS color palette member (`type BadgeColor`), not a LeafState.
- DO NOT touch `app/src/screens/PodcastScreen.tsx:220` — `'yellow'` there is a UI status color returned by `statusColor`, not a LeafState.
- DO NOT mix this commit with Plan 33-01 (TD-05) or 33-02 (TD-04) work.
- DO include `app/src/components/trellis/TrellisTooltip.tsx` in the same commit (has `case 'yellow'` / `case 'fallen'` branches that pinned tests require renamed).
- DO include code COMMENTS in the rename (prevents future grep confusion).

Purpose: D-06 via the LeafState rename — TD-06 dead-branch tsc error (originally at concept-feed.service.ts:745, pre-renamed to `'yellow'` in commit 760fa4f8, now at line 654 per researcher) becomes valid automatically once `'dying'` enters the type union.
Output: one atomic commit; `tsc -b --noEmit` stays exit 0; `grep "'yellow'" app/src/services/trellis-state.service.ts` returns 0; 24 pre-existing Node-25 trellis test failures stay unchanged (no new trellis regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md
@app/src/services/trellis-state.service.ts
@app/src/components/trellis/TrellisLeaf.tsx
@app/src/components/trellis/TrellisTooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Rename LeafState type union + computeLeafState + dev-mode seeds in trellis-state.service.ts</name>
  <read_first>
    - app/src/services/trellis-state.service.ts (lines 1-130 — entire file small; read fully)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #1 (exact line-by-line diff expected)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Code Example 1 (before/after snippet)
  </read_first>
  <action>
    Open `app/src/services/trellis-state.service.ts` and apply these precise edits:

    Edit A — line 9, the type union declaration:
    - BEFORE: `export type LeafState = 'bud' | 'green' | 'yellow' | 'falling' | 'fallen' | 'blossom' | 'fruit';`
    - AFTER:  `export type LeafState = 'bud' | 'green' | 'dying' | 'falling' | 'dead' | 'blossom' | 'fruit';`

    Edit B — line 88, computeLeafState return value for 14d overdue / ease<1.5:
    - BEFORE: `if (maxOverdue >= 14 || aggregateEase < 1.5) return 'fallen';`
    - AFTER:  `if (maxOverdue >= 14 || aggregateEase < 1.5) return 'dead';`

    Edit C — line 90, computeLeafState return value for 1d overdue:
    - BEFORE: `if (maxOverdue >= 1) return 'yellow';`
    - AFTER:  `if (maxOverdue >= 1) return 'dying';`

    Edit D — lines 113-121, dev-mode seed entries. Every occurrence of `state: 'yellow'` becomes `state: 'dying'`; every `state: 'fallen'` becomes `state: 'dead'`. Do NOT touch `state: 'falling'` (retained per D-12). Do NOT touch `state: 'green'`, `'bud'`, `'blossom'`, `'fruit'`. Approximate target lines per RESEARCH.md Finding #1:

    - Line 117 (contains `{ state: 'yellow', cat: 2, vineIdx: 2 }`): `'yellow'` → `'dying'`
    - Line 119 (contains `{ state: 'yellow', cat: 4, vineIdx: 3 }`): `'yellow'` → `'dying'`
    - Line 121 (contains `{ state: 'fallen', cat: 7, vineIdx: 4 }` and `{ state: 'yellow', cat: 6, vineIdx: 4 }`): `'fallen'` → `'dead'`; `'yellow'` → `'dying'`

    Strategy: use the `Edit` tool with each literal replacement as a SEPARATE call. Because `'yellow'` and `'fallen'` only appear in LeafState contexts in this file (grep confirms), global replace is safe — but make edits one string at a time and re-verify after each.

    Update any comments that reference the old vocabulary in this file for alignment with code (e.g., if line 110-112 has a comment mentioning "yellow" or "fallen" as LeafState states, update to "dying" / "dead"). Scan lines 1-130 for any such comments and update.

    Grep-verify post-edit:
    ```
    cd /Users/Code/EchoLearn && grep -n "'yellow'" app/src/services/trellis-state.service.ts
    # must return nothing
    grep -n "'fallen'" app/src/services/trellis-state.service.ts
    # must return nothing
    grep -n "'dying'" app/src/services/trellis-state.service.ts
    # must return at least 3 hits (union + computeLeafState + >=1 seed)
    grep -n "'dead'" app/src/services/trellis-state.service.ts
    # must return at least 2 hits (union + computeLeafState + maybe seed)
    grep -n "'falling'" app/src/services/trellis-state.service.ts
    # must still return at least 2 hits (retained per D-12)
    ```

    Do NOT commit — Task 3.6 makes the single atomic commit covering all rename touchpoints.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -c "'yellow'\|'fallen'" app/src/services/trellis-state.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'yellow'" app/src/services/trellis-state.service.ts` returns `0`.
    - `grep -c "'fallen'" app/src/services/trellis-state.service.ts` returns `0`.
    - `grep -c "'dying'" app/src/services/trellis-state.service.ts` returns at least `3`.
    - `grep -c "'dead'" app/src/services/trellis-state.service.ts` returns at least `2`.
    - `grep -c "'falling'" app/src/services/trellis-state.service.ts` returns at least `2` (retained).
    - Type union line exactly reads: `export type LeafState = 'bud' | 'green' | 'dying' | 'falling' | 'dead' | 'blossom' | 'fruit';`
  </acceptance_criteria>
  <done>trellis-state.service.ts renamed; downstream consumers will be updated next.</done>
</task>

<task type="auto">
  <name>Task 3.2: Update concept-feed.service.ts and PlannerScreen.tsx LeafState string comparisons</name>
  <read_first>
    - app/src/services/concept-feed.service.ts (locate the single LeafState comparison, post-760fa4f8 approximately at line 654 — per RESEARCH.md Finding #1)
    - app/src/screens/PlannerScreen.tsx (locate lines 46-47 with `leafState === 'fallen'` and `leafState === 'yellow' || leafState === 'falling'`)
  </read_first>
  <action>
    Step 1 — In `app/src/services/concept-feed.service.ts`, locate the single LeafState predicate. Per researcher it reads:

    `isImportant = leaf === 'yellow' || leaf === 'falling' || leaf === 'fallen';`

    Find it via:
    `grep -n "leaf === 'yellow'\\|leaf === 'fallen'" app/src/services/concept-feed.service.ts`

    Replace the line exactly with:

    `isImportant = leaf === 'dying' || leaf === 'falling' || leaf === 'dead';`

    Note: this file previously triggered TD-06's TS2367 (dead `'dying'` branch); post-rename the literal is valid. `'falling'` stays unchanged.

    Step 2 — In `app/src/screens/PlannerScreen.tsx`, locate lines 46-47. Current text:

    ```
    const deadNodes = layout.nodes.filter((n) => n.leafState === 'fallen');
    const dyingNodes = layout.nodes.filter((n) => n.leafState === 'yellow' || n.leafState === 'falling');
    ```

    Replace with:

    ```
    const deadNodes = layout.nodes.filter((n) => n.leafState === 'dead');
    const dyingNodes = layout.nodes.filter((n) => n.leafState === 'dying' || n.leafState === 'falling');
    ```

    Step 3 — Grep-verify:
    ```
    cd /Users/Code/EchoLearn
    grep -n "leaf === 'yellow'\|leaf === 'fallen'\|leafState === 'yellow'\|leafState === 'fallen'" app/src/services/concept-feed.service.ts app/src/screens/PlannerScreen.tsx
    # must return zero hits

    grep -n "leaf === 'dying'\|leafState === 'dying'" app/src/services/concept-feed.service.ts app/src/screens/PlannerScreen.tsx
    # must return at least 2 hits
    ```

    Do NOT commit yet.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -c "'yellow'\|'fallen'" app/src/services/concept-feed.service.ts app/src/screens/PlannerScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'yellow'" /Users/Code/EchoLearn/app/src/services/concept-feed.service.ts` returns `0`.
    - `grep -c "'fallen'" /Users/Code/EchoLearn/app/src/services/concept-feed.service.ts` returns `0`.
    - `grep -c "leafState === 'yellow'\|leafState === 'fallen'" /Users/Code/EchoLearn/app/src/screens/PlannerScreen.tsx` returns `0`.
    - `grep -c "leafState === 'dying'\|leafState === 'dead'" /Users/Code/EchoLearn/app/src/screens/PlannerScreen.tsx` returns at least `2`.
  </acceptance_criteria>
  <done>concept-feed.service.ts + PlannerScreen.tsx aligned with new LeafState vocabulary.</done>
</task>

<task type="auto">
  <name>Task 3.3: Update TrellisStatusPanel.tsx, TrellisLeaf.tsx, and TrellisTooltip.tsx</name>
  <read_first>
    - app/src/components/trellis/TrellisStatusPanel.tsx (lines 40-60 to confirm dyingNodes/deadNodes filter predicates)
    - app/src/components/trellis/TrellisLeaf.tsx (lines 70-90 for STATE_COLOR/STATE_VEIN, lines 510-530 for withDecay signature + branches)
    - app/src/components/trellis/TrellisTooltip.tsx (whole file — locate `case 'yellow'` and `case 'fallen'` in resolveHealthCopy and any mapping functions)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #1 (TrellisLeaf.tsx site list lines 73, 75, 83, 85, 516, 517, 518, 525)
  </read_first>
  <action>
    Step 1 — `app/src/components/trellis/TrellisStatusPanel.tsx` lines 44-45:

    - BEFORE line 44: `const dyingNodes = nodes.filter((n) => n.leafState === 'yellow' || n.leafState === 'falling');`
    - AFTER line 44: `const dyingNodes = nodes.filter((n) => n.leafState === 'dying' || n.leafState === 'falling');`
    - BEFORE line 45: `const deadNodes = nodes.filter((n) => n.leafState === 'fallen');`
    - AFTER line 45: `const deadNodes = nodes.filter((n) => n.leafState === 'dead');`

    Step 2 — `app/src/components/trellis/TrellisLeaf.tsx`. Apply these edits:

    - Line 73 (STATE_COLOR Record key): `yellow: '#F9A825',` → `dying: '#F9A825',`
    - Line 75 (STATE_COLOR Record key): `fallen: '#8D6E63',` → `dead: '#8D6E63',`
    - Line 83 (STATE_VEIN Record key): `yellow: '#F57F17',` → `dying: '#F57F17',`
    - Line 85 (STATE_VEIN Record key): `fallen: '#5D4037',` → `dead: '#5D4037',`
    - Line 516 (withDecay fn signature): `function withDecay(leaf: React.ReactNode, state: 'yellow' | 'falling' | 'fallen'): React.ReactNode {` → `function withDecay(leaf: React.ReactNode, state: 'dying' | 'falling' | 'dead'): React.ReactNode {`
    - Line 517: `const extraRotation = state === 'falling' ? 20 : state === 'fallen' ? -15 : 0;` → `const extraRotation = state === 'falling' ? 20 : state === 'dead' ? -15 : 0;`
    - Line 518: `const opacity = state === 'fallen' ? 0.75 : 1;` → `const opacity = state === 'dead' ? 0.75 : 1;`
    - Line 525: `{state === 'fallen' && (` → `{state === 'dead' && (`

    Also update comments in the file referring to the old vocabulary so that comments stay aligned with code. Specifically:
    - Line 514 comment (approximately): "Yellow/falling/fallen wrap the base leaf with visual decay cues." → "Dying/falling/dead wrap the base leaf with visual decay cues."
    - Line 570 comment (approximately): "green, yellow, falling, fallen — use category leaf shape with state color" → "green, dying, falling, dead — use category leaf shape with state color"

    (Line numbers are approximate — apply via exact-text match. If grep doesn't find the comments at 514/570, search for the text strings and update wherever found.)

    Step 3 — `app/src/components/trellis/TrellisTooltip.tsx`:

    Open the file and find the `resolveHealthCopy` function. It likely has a switch statement or object-map with `case 'yellow':` / `case 'fallen':` branches (per RESEARCH.md Pitfall #5). For each:
    - `case 'yellow':` → `case 'dying':`
    - `case 'fallen':` → `case 'dead':`

    Also scan the file for any object-literal keys `yellow: ...` or `fallen: ...` in LeafState-to-copy Records, and rename those keys to `dying` / `dead`.

    If the file uses a `Record<LeafState, string>` type, the rename propagates automatically through the type from trellis-state.service.ts — but the object literal keys must still be manually renamed to match the new type's expected key names.

    Step 4 — Global grep sweep to confirm no stray `'yellow'`/`'fallen'` remains in trellis code (excluding Badge.tsx and PodcastScreen.tsx which are intentionally untouched):

    ```
    cd /Users/Code/EchoLearn && grep -rn "'yellow'\|'fallen'" app/src/components/trellis/ app/src/screens/PlannerScreen.tsx app/src/services/trellis-state.service.ts app/src/services/concept-feed.service.ts
    # must return zero hits
    ```

    Do NOT commit yet.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -rn "'yellow'\|'fallen'" app/src/components/trellis/ app/src/screens/PlannerScreen.tsx app/src/services/trellis-state.service.ts app/src/services/concept-feed.service.ts | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - Above grep sweep returns `0` total hits.
    - `grep -c "dying:" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx` returns at least `2` (STATE_COLOR + STATE_VEIN keys).
    - `grep -c "dead:" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx` returns at least `2`.
    - TrellisLeaf.tsx `withDecay` signature reads: `state: 'dying' | 'falling' | 'dead'`.
    - TrellisTooltip.tsx contains `case 'dying'` or `dying:` key at least once, and `case 'dead'` or `dead:` key at least once.
  </acceptance_criteria>
  <done>Trellis components + tooltip all aligned; awaiting test updates + atomic commit.</done>
</task>

<task type="auto">
  <name>Task 3.4: Update test fixtures — trellis-state.test.mjs, trellis-tooltip-copy.test.mjs, concept-batch-filter.test.mjs</name>
  <read_first>
    - app/tests/services/trellis-state.test.mjs (lines 40-65 per RESEARCH.md Finding #1 — rename assertion args and test names)
    - app/tests/components/trellis-tooltip-copy.test.mjs (lines 10-30 — rename resolveHealthCopy args and the LeafState enumeration)
    - app/tests/services/concept-batch-filter.test.mjs (line 15 — comment only, per researcher)
  </read_first>
  <action>
    Step 1 — `app/tests/services/trellis-state.test.mjs`:

    - Line 44 (test name): `'computeLeafState returns yellow for 1-7 day overdue child'` → `'computeLeafState returns dying for 1-7 day overdue child'`
    - Line 49: `assert.equal(computeLeafState(anchor, [child]), 'yellow');` → `assert.equal(computeLeafState(anchor, [child]), 'dying');`
    - Line 58: `assert.equal(computeLeafState(anchor, [bad, good]), 'fallen');` → `assert.equal(computeLeafState(anchor, [bad, good]), 'dead');`
    - Line number: scan ENTIRE file for any remaining `'yellow'` or `'fallen'` strings; rename each.

    Step 2 — `app/tests/components/trellis-tooltip-copy.test.mjs`:

    - Line 15: `assert.equal(mod.resolveHealthCopy('yellow', 2, 0), '...')` → `assert.equal(mod.resolveHealthCopy('dying', 2, 0), '...')` (preserve the string `'...'` — that's the pinned expected copy)
    - Line 17: `assert.equal(mod.resolveHealthCopy('fallen', 3, 0), '...')` → `assert.equal(mod.resolveHealthCopy('dead', 3, 0), '...')`
    - Line 25 (LeafState enumeration): `const expected = ['bud', 'green', 'yellow', 'falling', 'fallen', 'blossom', 'fruit'];` → `const expected = ['bud', 'green', 'dying', 'falling', 'dead', 'blossom', 'fruit'];`

    Step 3 — `app/tests/services/concept-batch-filter.test.mjs`:

    - Line 15 (comment): `"ease < 1.5 or dying/falling/fallen"` → `"ease < 1.5 or dying/falling/dead"`.

    This is a comment-only update for consistency. (Note: researcher verified this test landed in the WIP flush commit per Plan 33-00 — editing it here is fine since we control the rename window.)

    Step 4 — Grep sweep across `app/tests/`:
    ```
    cd /Users/Code/EchoLearn && grep -rn "'yellow'\|'fallen'" app/tests/
    # Any remaining hits should be investigated — expected: zero LeafState-context hits
    ```

    If any hit remains, inspect whether it's a genuine LeafState reference (rename) or incidental text (leave).

    Do NOT commit yet.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -rn "'yellow'\|'fallen'" app/tests/ | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "'yellow'\\|'fallen'" /Users/Code/EchoLearn/app/tests/` returns `0` hits.
    - `grep -c "'dying'" /Users/Code/EchoLearn/app/tests/services/trellis-state.test.mjs` returns at least `1`.
    - `grep -c "'dead'" /Users/Code/EchoLearn/app/tests/services/trellis-state.test.mjs` returns at least `1`.
    - `grep -c "resolveHealthCopy('dying'" /Users/Code/EchoLearn/app/tests/components/trellis-tooltip-copy.test.mjs` returns at least `1`.
    - `grep -c "resolveHealthCopy('dead'" /Users/Code/EchoLearn/app/tests/components/trellis-tooltip-copy.test.mjs` returns at least `1`.
    - `grep "dying/falling/dead" /Users/Code/EchoLearn/app/tests/services/concept-batch-filter.test.mjs` returns a hit.
  </acceptance_criteria>
  <done>Test fixtures renamed; ready for verification + commit.</done>
</task>

<task type="auto">
  <name>Task 3.5: Verify tsc, global grep-sweep, and test baseline — then make the single atomic commit</name>
  <read_first>
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #8 (test baseline: 26 pre-existing Node-25 trellis failures expected)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Pitfall #4 (trellis tests fail on import-attribute error, not rename error)
  </read_first>
  <action>
    Step 1 — Run tsc:
    ```
    cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit
    ```
    Must exit 0 (no errors). If any TS2367 or type-incompatibility error surfaces, investigate — likely a missed LeafState callsite.

    Step 2 — Global grep sweep across the whole `app/` tree. Confirm only Badge.tsx and PodcastScreen.tsx still contain `'yellow'` (both intentional per D-13 scope boundaries):

    ```
    cd /Users/Code/EchoLearn && grep -rn "'yellow'" app/src/ app/tests/ --include="*.ts" --include="*.tsx" --include="*.mjs" --include="*.js"
    ```

    Expected output: ONLY hits in `app/src/components/ui/Badge.tsx` and `app/src/screens/PodcastScreen.tsx`. If any hit appears outside those two files, investigate and fix.

    ```
    cd /Users/Code/EchoLearn && grep -rn "'fallen'" app/src/ app/tests/ --include="*.ts" --include="*.tsx" --include="*.mjs" --include="*.js"
    ```

    Expected output: zero hits.

    ```
    cd /Users/Code/EchoLearn && grep -rn "'falling'" app/src/ app/tests/ --include="*.ts" --include="*.tsx" --include="*.mjs" --include="*.js" | wc -l
    ```

    Expected: at least 3 (retained per D-12).

    Step 3 — Run the full test suite to establish the post-rename baseline:
    ```
    cd /Users/Code/EchoLearn/app && npm test 2>&1 | tail -8
    ```

    Expected (per RESEARCH.md Finding #8):
    - After Plan 33-00: 377 tests / 345 pass / 32 fail
    - After Plan 33-02 (TD-04 deletions landed): ~366 tests / ~340 pass / ~26 fail (6 fewer v1.4 regressions)
    - After this plan (TD-06 rename): same ~366 test count, ~26 fail (rename doesn't add new failures — pre-existing Node-25 trellis import-attribute failures stay exactly the same count)

    If fail count drops to 20-26 range (pre-existing Node-25 only), pass. If fail count ≥ 30 or new non-import-attribute failures appear, STOP — a rename callsite was missed.

    Step 4 — Run vite build to confirm no build regression:
    ```
    cd /Users/Code/EchoLearn/app && npx vite build 2>&1 | tail -5
    ```
    Expected: clean build in ~3s.

    Step 5 — Stage + commit all 9 files atomically per D-15:
    ```
    cd /Users/Code/EchoLearn && git add \
      app/src/services/trellis-state.service.ts \
      app/src/services/concept-feed.service.ts \
      app/src/screens/PlannerScreen.tsx \
      app/src/components/trellis/TrellisStatusPanel.tsx \
      app/src/components/trellis/TrellisLeaf.tsx \
      app/src/components/trellis/TrellisTooltip.tsx \
      app/tests/services/trellis-state.test.mjs \
      app/tests/components/trellis-tooltip-copy.test.mjs \
      app/tests/services/concept-batch-filter.test.mjs

    git commit -m "$(cat <<'EOF'
    refactor(trellis): rename LeafState literals yellow->dying, fallen->dead per design vocabulary (TD-06)

    Unify internal LeafState vocabulary with user-facing design labels.
    UI already used "Dying" / "Dead" (TrellisStatusPanel filters); the literal
    type members lagged.

    Per Phase 33 D-10/D-11/D-12/D-13/D-15:
    - 'yellow' -> 'dying' (1-6d overdue)
    - 'fallen' -> 'dead'  (14d+ overdue or ease<1.5)
    - 'falling' RETAINED — internal-only 7-13d gradation, UI never exposes it

    Edits (atomic per D-15):
    - trellis-state.service.ts: LeafState type union + computeLeafState return
      values + dev-mode seed array entries + aligned comments.
    - concept-feed.service.ts: weak-concept predicate (leaf === 'dying' || ...
      'falling' || 'dead'). This was the TD-06 TS2367 site (dead 'dying'
      branch pre-renamed to 'yellow' in 760fa4f8); the new literal is now
      valid.
    - PlannerScreen.tsx: dyingNodes / deadNodes filter predicates.
    - trellis/TrellisStatusPanel.tsx: dyingNodes / deadNodes filter predicates.
    - trellis/TrellisLeaf.tsx: STATE_COLOR + STATE_VEIN Record keys; withDecay
      signature; 3 literal comparisons; aligned comments.
    - trellis/TrellisTooltip.tsx: resolveHealthCopy case 'dying' / case 'dead'.
    - tests/services/trellis-state.test.mjs: rename test names + assertion values.
    - tests/components/trellis-tooltip-copy.test.mjs: rename resolveHealthCopy
      args + LeafState enumeration.
    - tests/services/concept-batch-filter.test.mjs: comment-only alignment.

    Intentionally NOT renamed per D-13 scope:
    - Badge.tsx 'yellow' — CSS color palette, independent of trellis.
    - PodcastScreen.tsx:220 'yellow' — UI status color, not LeafState.

    Locale bundles unchanged — planner.trellis.dying / planner.trellis.dead
    keys already exist with correct copy (D-14).

    tsc -b --noEmit: exit 0. vite build: clean. Test delta: no new failures
    (26 pre-existing Node-25 import-attribute failures remain; not a rename
    regression per Pitfall #4).
    EOF
    )"
    ```

    Step 6 — Final verify:
    ```
    cd /Users/Code/EchoLearn && git status --porcelain
    # must be empty
    git log -1 --format=%s
    # must start with "refactor(trellis): rename LeafState"
    git log -1 --stat | grep -c "app/"
    # must be >= 9 (9 changed files)
    ```
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit && echo "tsc-clean"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b --noEmit` exits 0 with empty output.
    - Global grep of `'yellow'` in `app/src/` / `app/tests/` returns hits ONLY in `Badge.tsx` and `PodcastScreen.tsx`.
    - Global grep of `'fallen'` in `app/src/` / `app/tests/` returns zero hits.
    - `grep -c "'falling'"` across same scope returns at least 3 (retained).
    - `npm test` final count: fail count within 0-6 of the post-Plan-33-02 baseline (rename must not add failures beyond the 26 pre-existing Node-25 ones).
    - `npx vite build` exits 0.
    - `git log -1 --format=%s` starts with `refactor(trellis): rename LeafState`.
    - `git log -1 --stat` shows all 9 files in the single commit.
    - `git status --porcelain` is empty.
  </acceptance_criteria>
  <done>LeafState rename complete in one atomic commit; tsc clean; test baseline preserved.</done>
</task>

</tasks>

<verification>
- `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo $?` returns `0`.
- `cd /Users/Code/EchoLearn/app && npx vite build` completes cleanly.
- Global `grep "'yellow'\|'fallen'"` across `app/src` + `app/tests` returns ONLY the 2 intentional Badge.tsx / PodcastScreen.tsx `'yellow'` hits (no `'fallen'` at all).
- `npm test` fail count stays at or below post-Plan-33-02 baseline.
- Single commit covers all 9 files — `git log --follow app/src/services/trellis-state.service.ts` traces cleanly.
</verification>

<success_criteria>
- 9 files renamed in one atomic commit.
- tsc clean, vite build clean.
- No new trellis test regressions (pre-existing Node-25 count preserved).
- Badge.tsx + PodcastScreen.tsx confirmed untouched.
- `git log --follow` on the renamed files shows a single refactor commit.
</success_criteria>

<output>
After completion, create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-03-SUMMARY.md` with the commit SHA, exhaustive rename site list, tsc output, vite build output, and test-delta measurement vs. Plan 33-02 baseline.
</output>
