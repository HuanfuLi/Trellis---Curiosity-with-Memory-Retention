---
phase: 51-concept-dashboard-and-recovery-surfaces
plan: 03
type: execute
wave: 2
depends_on: ["51-02"]
files_modified:
  - app/src/screens/PostDetailScreen.tsx
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/tests/screens/PostDetailScreen.conceptNav.test.mjs
autonomous: true
requirements: [RETRIEVE-03, RETRIEVE-04]
must_haves:
  truths:
    - "PostDetailScreen contextLabel line (~970) is tappable — navigates to /anchor/:anchorId when anchorId can be resolved from post.sourceQuestionIds"
    - "Connection post concept pills (conceptNounA/B at ~787) are tappable — each navigates to /anchor/:anchorId for the respective question's parent anchor"
    - "Anchor ID resolution: post.sourceQuestionIds[0] → questionService.getById(id) → question.parentId (the anchor node ID). If parent is not an anchor node, walk up one more level"
    - "When anchorId cannot be resolved (sourceQuestionIds empty or questions deleted), the label/pill renders as non-interactive static text (no broken navigation)"
    - "All 4 locale bundles (en/zh/es/ja) have identical key sets for the new conceptDashboard namespace — bundle-parity.test.mjs passes"
    - "No existing PostDetailScreen behavior is changed — video detectors A-D, Q&A follow-up, news attribution all untouched"
  artifacts:
    - path: app/src/screens/PostDetailScreen.tsx
      provides: "Concept label and connection pills are tappable entry points to concept dashboard"
      contains: "navigate(`/anchor/"
    - path: app/src/locales/zh.json
      provides: "Chinese translations for conceptDashboard namespace"
      contains: "conceptDashboard"
    - path: app/src/locales/es.json
      provides: "Spanish translations for conceptDashboard namespace"
      contains: "conceptDashboard"
    - path: app/src/locales/ja.json
      provides: "Japanese translations for conceptDashboard namespace"
      contains: "conceptDashboard"
    - path: app/tests/screens/PostDetailScreen.conceptNav.test.mjs
      provides: "Concept navigation entry point tests"
      contains: "describe('PostDetailScreen concept navigation'"
  key_links:
    - from: app/src/screens/PostDetailScreen.tsx
      to: app/src/screens/AnchorDetailScreen.tsx
      via: "navigate(`/anchor/${anchorId}`) — entry point to concept dashboard"
    - from: app/src/screens/PostDetailScreen.tsx
      to: app/src/services/question.service.ts
      via: "questionService.getById(sourceQuestionIds[0]) to resolve parent anchor"
---

<objective>
Add tappable concept entry points to PostDetailScreen (the main missing RETRIEVE-03 navigation path) and complete the i18n bundles for all Phase 51 keys. PostDetailScreen currently shows concept context as static text — this plan makes it a navigation link to the concept dashboard.

Purpose: RETRIEVE-03 requires "User can open a concept dashboard from concept-linked surfaces." PostDetailScreen is the most common concept-linked surface (every post shows its concept context). Making the contextLabel and connection pills tappable closes the primary entry point gap.

Output: Modified PostDetailScreen.tsx, all 4 locale bundles updated, concept navigation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-concept-dashboard-and-recovery-surfaces/51-RESEARCH.md

<interfaces>
<!-- PostDetailScreen concept label (line ~970) — currently static text -->
<p style={{ fontSize: '0.82rem', ...muted styles... }}>
  {normalizedContextLabel} · {post.narrativeMode}
</p>

<!-- Connection pills (lines ~784-802) — currently static spans -->
{[meta.conceptNounA, meta.conceptNounB].map((noun, i) => (
  <span key={i} style={{ ...pill styles... }}>{noun}</span>
))}

<!-- Anchor ID resolution path -->
post.sourceQuestionIds: string[]  (Q&A node IDs, NOT anchor IDs)
questionService.getById(qaId): Question | undefined
question.parentId: string  (anchor node ID for leaf Q&As)
question.isAnchorNode: boolean  (true if the parent IS the anchor)

Resolution: for a sourceQuestionId, getById → check if parent isAnchorNode. If yes, navigate to parent. If parent is a Q&A (not anchor), its parent should be the anchor (two hops max in the tree).

<!-- i18n: EN keys added in Plan 51-02, Task 3 (conceptDashboard namespace) -->
Sonnet subagent prompt: app/scripts/translate-locales.md
Locales: zh (Simplified Chinese), es (Spanish), ja (Japanese)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Make PostDetailScreen contextLabel and connection pills tappable with anchor navigation</name>
  <files>app/src/screens/PostDetailScreen.tsx</files>
  <read_first>
    - app/src/screens/PostDetailScreen.tsx (full file — understand all concept-related rendering: contextLabel at ~970, connection pills at ~784-802, sourceQuestionIds usage, existing imports)
    - app/src/services/question.service.ts (getById signature — verify it returns Question | undefined)
    - app/src/types/index.ts (Question type — verify parentId, isAnchorNode fields exist)
  </read_first>
  <behavior>
    - Non-connection posts: the `{normalizedContextLabel} · {post.narrativeMode}` text at ~line 970 becomes a tappable element. Tapping the contextLabel portion navigates to `/anchor/${resolvedAnchorId}`. The ` · {post.narrativeMode}` part remains static.
    - Connection posts: each concept pill (conceptNounA, conceptNounB) becomes tappable. Pill A navigates to the anchor of `connectionMeta.questionA`, pill B to anchor of `connectionMeta.questionB`.
    - Anchor resolution function (local helper, NOT exported):
      ```typescript
      function resolveAnchorId(sourceQuestionId: string): string | null {
        const qa = questionService.getById(sourceQuestionId);
        if (!qa) return null;
        if (qa.isAnchorNode) return qa.id; // sourceQuestionId IS the anchor
        const parent = qa.parentId ? questionService.getById(qa.parentId) : null;
        if (parent?.isAnchorNode) return parent.id;
        // Two hops didn't find anchor — return null (graceful degradation)
        return null;
      }
      ```
    - Compute anchorId(s) with useMemo based on post.sourceQuestionIds / connectionMeta
    - If anchorId is null, render as static text (unchanged visual, no cursor: pointer, no onClick)
    - If anchorId is resolved, render contextLabel as a `<button>` styled like a link (color: primary-40, cursor: pointer, text-decoration: underline on hover) and connection pills keep their existing visual but gain onClick + cursor: pointer
    - No new imports needed beyond questionService (already in scope — check)
    - CRITICAL: Do NOT change any video detector logic, Q&A follow-up, news attribution, or any other behavior
  </behavior>
  <action>
Modify `app/src/screens/PostDetailScreen.tsx`:

1. **Import questionService** if not already imported:
   Check existing imports. If `questionService` is not imported, add: `import { questionService } from '../services/question.service';`

2. **Add resolveAnchorId helper** above the component or inside it as a stable function:
   ```typescript
   function resolveAnchorId(sourceQuestionId: string): string | null {
     const qa = questionService.getById(sourceQuestionId);
     if (!qa) return null;
     if (qa.isAnchorNode) return qa.id;
     const parent = qa.parentId ? questionService.getById(qa.parentId) : null;
     if (parent?.isAnchorNode) return parent.id;
     return null;
   }
   ```

3. **Compute anchor IDs for the current post** (inside the component, after `post` is confirmed non-null):
   ```typescript
   const conceptAnchorId = useMemo(() => {
     if (!post.sourceQuestionIds.length) return null;
     return resolveAnchorId(post.sourceQuestionIds[0]);
   }, [post.sourceQuestionIds]);

   // For connection posts — resolve both anchors
   const connectionAnchorIds = useMemo(() => {
     if (!isConnectionPost || !meta) return [null, null] as const;
     return [
       resolveAnchorId(meta.questionA.id),
       resolveAnchorId(meta.questionB.id),
     ] as const;
   }, [isConnectionPost, meta]);
   ```

4. **Modify contextLabel rendering (~line 970):**
   Replace the static `<p>` with:
   ```tsx
   <p style={{ fontSize: '0.82rem', color: 'var(--muted-foreground)', marginBottom: '2px' }}>
     {conceptAnchorId ? (
       <>
         <button
           onClick={() => navigate(`/anchor/${conceptAnchorId}`)}
           style={{
             background: 'none',
             border: 'none',
             padding: 0,
             color: 'var(--primary-40)',
             fontWeight: 600,
             fontSize: 'inherit',
             cursor: 'pointer',
             textDecoration: 'none',
           }}
         >
           {normalizedContextLabel}
         </button>
         {' · '}{post.narrativeMode}
       </>
     ) : (
       <>{normalizedContextLabel} · {post.narrativeMode}</>
     )}
   </p>
   ```

5. **Modify connection pills (~lines 784-802):**
   Replace the static `<span>` with:
   ```tsx
   {[meta.conceptNounA, meta.conceptNounB].map((noun, i) => {
     const anchorId = connectionAnchorIds[i];
     const pill = (
       <span
         key={i}
         role={anchorId ? 'button' : undefined}
         tabIndex={anchorId ? 0 : undefined}
         onClick={anchorId ? () => navigate(`/anchor/${anchorId}`) : undefined}
         style={{
           padding: '6px 16px',
           borderRadius: '100px',
           backgroundColor: i === 0 ? 'var(--node-mint)' : 'var(--node-sky)',
           color: 'var(--foreground)',
           fontWeight: 700,
           fontSize: '0.875rem',
           cursor: anchorId ? 'pointer' : 'default',
         }}
       >
         {noun}
       </span>
     );
     return pill;
   })}
   ```

CRITICAL: Only modify the contextLabel `<p>` and connection pills `<span>`. Do NOT touch video detectors (A, B, C, D), Q&A follow-up handler, news source attribution, essay streaming, or any other PostDetailScreen functionality.
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generate zh/es/ja translations for conceptDashboard namespace</name>
  <files>
    app/src/locales/zh.json
    app/src/locales/es.json
    app/src/locales/ja.json
  </files>
  <read_first>
    - app/src/locales/en.json (read the conceptDashboard namespace added in Plan 51-02 Task 3 — these are the keys to translate)
    - app/src/locales/zh.json (find where to insert conceptDashboard block — after the graph namespace, matching en.json structure)
    - app/src/locales/es.json (same)
    - app/src/locales/ja.json (same)
    - app/scripts/translate-locales.md (the Sonnet subagent prompt for translations)
  </read_first>
  <behavior>
    - All 3 non-EN locale files have the conceptDashboard namespace with identical key sets as en.json
    - Translations are natural and concise for each locale
    - Proper nouns NOT translated: Trellis, Q&A (keep as-is in all locales)
    - Interpolation placeholders preserved exactly: {{count}}, {{total}}, {{due}}, {{minutes}}
    - `bundle-parity.test.mjs` passes after changes
  </behavior>
  <action>
Use the Sonnet subagent translation workflow (app/scripts/translate-locales.md) to generate translations.

If unable to spawn the subagent, translate manually using these guidelines:

**zh (Simplified Chinese):**
```json
"conceptDashboard": {
  "tabs": { "overview": "概览", "posts": "帖子", "reviews": "复习", "podcasts": "播客" },
  "posts": { "heading": "帖子", "viewAll": "查看全部 {{count}} 篇帖子", "empty": "该概念暂无相关帖子", "saved": "已保存", "liked": "已点赞", "inCollections": "在 {{count}} 个合集中" },
  "reviews": { "heading": "闪卡复习", "stats": "{{total}} 张闪卡，{{due}} 张待复习", "reviewNow": "复习 {{count}} 张待复习卡", "empty": "暂无闪卡 — 将从你的问答中自动生成", "due": "待复习" },
  "podcasts": { "heading": "播客提及", "empty": "暂无播客提及此概念", "duration": "{{minutes}} 分钟", "concepts": "{{count}} 个概念" },
  "leafState": { "bud": "新概念", "green": "健康", "dying": "需要复习", "falling": "逾期", "dead": "不活跃", "blossom": "已掌握", "fruit": "待收获" }
}
```

**es (Spanish):**
```json
"conceptDashboard": {
  "tabs": { "overview": "Resumen", "posts": "Publicaciones", "reviews": "Repasos", "podcasts": "Podcasts" },
  "posts": { "heading": "Publicaciones", "viewAll": "Ver las {{count}} publicaciones", "empty": "Aún no hay publicaciones para este concepto", "saved": "Guardado", "liked": "Me gusta", "inCollections": "En {{count}} colecciones" },
  "reviews": { "heading": "Repasos de tarjetas", "stats": "{{total}} tarjetas, {{due}} pendientes de repaso", "reviewNow": "Repasar {{count}} tarjetas pendientes", "empty": "Aún no hay tarjetas — se generarán a partir de tus preguntas", "due": "Pendiente" },
  "podcasts": { "heading": "Menciones en podcasts", "empty": "Ningún podcast menciona este concepto aún", "duration": "{{minutes}} min", "concepts": "{{count}} conceptos" },
  "leafState": { "bud": "Nuevo", "green": "Saludable", "dying": "Necesita repaso", "falling": "Atrasado", "dead": "Inactivo", "blossom": "Dominado", "fruit": "Listo para cosechar" }
}
```

**ja (Japanese):**
```json
"conceptDashboard": {
  "tabs": { "overview": "概要", "posts": "投稿", "reviews": "復習", "podcasts": "ポッドキャスト" },
  "posts": { "heading": "投稿", "viewAll": "全{{count}}件の投稿を見る", "empty": "このコンセプトの投稿はまだありません", "saved": "保存済み", "liked": "いいね済み", "inCollections": "{{count}}個のコレクションに含まれる" },
  "reviews": { "heading": "フラッシュカード復習", "stats": "{{total}}枚のカード、{{due}}枚が復習待ち", "reviewNow": "{{count}}枚の復習待ちカードを復習", "empty": "フラッシュカードはまだありません — Q&Aから自動生成されます", "due": "復習待ち" },
  "podcasts": { "heading": "ポッドキャストでの言及", "empty": "このコンセプトに言及するポッドキャストはまだありません", "duration": "{{minutes}}分", "concepts": "{{count}}個のコンセプト" },
  "leafState": { "bud": "新規", "green": "健全", "dying": "復習が必要", "falling": "期限超過", "dead": "非アクティブ", "blossom": "習得済み", "fruit": "収穫可能" }
}
```

Insert the `conceptDashboard` block at the same position as in en.json (after the `graph` namespace).

After inserting, run:
```bash
cd app && node --test tests/locales/bundle-parity.test.mjs
```
Fix any key mismatches until the test passes.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Concept navigation tests + UAT checklist verification</name>
  <files>app/tests/screens/PostDetailScreen.conceptNav.test.mjs</files>
  <read_first>
    - app/tests/screens/PostDetailScreen.detectors.test.mjs or any PostDetailScreen test (test pattern for this screen)
    - app/tests/components/InfoFlow.video-tap-emit.test.mjs (negative assertion pattern)
    - app/src/screens/PostDetailScreen.tsx (the modified file — verify concept navigation code is in place)
  </read_first>
  <behavior>
    - Test 1: Non-connection post with resolvable anchorId → contextLabel renders as button with onClick navigating to /anchor/:anchorId
    - Test 2: Non-connection post with empty sourceQuestionIds → contextLabel renders as static text (no button)
    - Test 3: Connection post → both concept pills render with onClick navigating to respective anchor IDs
    - Test 4: Connection post where one anchor is deleted → that pill renders as static span, other pill is tappable
    - Test 5: resolveAnchorId walks up two hops correctly (Q&A → parent → anchor)
    - Test 6 (negative): Video detectors (enablejsapi, postMessage listener) are NOT affected by concept navigation changes
  </behavior>
  <action>
Create `app/tests/screens/PostDetailScreen.conceptNav.test.mjs`.

Follow the project's node:test + esbuild tsx loader pattern.

Test the `resolveAnchorId` logic by mocking `questionService.getById`:
- Mock returns a Q&A with `parentId: 'anchor-1'` for a source question ID
- Mock returns an anchor with `isAnchorNode: true` for 'anchor-1'
- Verify the resolution chain works

For the navigation tests, verify that the modified JSX includes the navigate call pattern by source-reading or render-testing.

For the negative assertion (Test 6), verify that `enablejsapi=1` still appears in the file (source read) — same pattern as InfoFlow.video-tap-emit.test.mjs.

**UAT checklist** (executor runs this after all tests pass):
1. Navigate to a post → tap concept label → arrives at concept dashboard (/anchor/:id)
2. Navigate to a connection post → tap concept pill A → arrives at correct anchor
3. Concept dashboard Overview tab shows existing Q&A content + leaf state badge
4. Posts tab shows concept's posts with saved/liked indicators
5. Reviews tab shows flashcard stats + "Review Now" CTA works
6. Podcasts tab shows podcast mentions
7. Tab resets to Overview when navigating to a different concept
8. All 4 locale bundles pass bundle-parity.test.mjs
9. `npm test` passes (full suite)
  </action>
</task>

</tasks>
