import assert from 'node:assert/strict';
import test from 'node:test';

// TODO: Plan 27-02 — replace this placeholder with assertions that
// formatDate/formatDateLabel/getGreeting honor the current i18n locale
// (expect month abbreviations to differ between en and ja).
test('placeholder — replaced by Plan 27-02', () => {
  assert.ok(true, 'skeleton only');
});
