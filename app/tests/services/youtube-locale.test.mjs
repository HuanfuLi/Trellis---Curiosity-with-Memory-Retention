import assert from 'node:assert/strict';
import test from 'node:test';

// TODO: Plan 27-02 — replace with assertion that YouTube Data API URL
// contains hl=, regionCode=, relevanceLanguage= params derived from the
// current i18n locale (e.g. hl=zh-CN&regionCode=CN&relevanceLanguage=zh
// when locale === 'zh').
test('placeholder — replaced by Plan 27-02', () => {
  assert.ok(true, 'skeleton only');
});
