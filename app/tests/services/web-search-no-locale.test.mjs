import assert from 'node:assert/strict';
import test from 'node:test';

// TODO: Plan 27-02 — replace with assertion that the Tavily web-search
// request body NEVER contains locale markers (zh/es/ja) per D-15 — web
// search is intentionally English/global.
test('placeholder — replaced by Plan 27-02', () => {
  assert.ok(true, 'skeleton only');
});
